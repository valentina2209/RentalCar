import Loader from "../../components/Loader/Loader";

// ...

return (
  <div className={styles.catalogWrapper}>
    <Filters />

    <CarList cars={cars} />

    {isLoading && <Loader />} {/* показуємо крутилку */}

    {hasMore && !isLoading && (
      <button
        className={styles.loadMoreBtn}
        onClick={handleLoadMore}
      >
        Load more
      </button>
    )}
  </div>
);
